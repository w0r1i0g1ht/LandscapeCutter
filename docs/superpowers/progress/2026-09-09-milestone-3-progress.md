# 里程碑 3 执行进度

状态：已完成。六类标注工具、统一输出和人工反馈修复已在 2026-09-14 获得用户验收并合并到
`main`；真实多显示器、负坐标、混合 DPI 和 HDR 仍属于后续兼容性覆盖，不阻塞本里程碑收口。

## 已实现

- 六类标注工具：矩形、椭圆、箭头、画笔、文字和马赛克；对象支持选择、移动、调整、删除、撤销和重做。
- 预览、剪贴板、PNG 与 JPEG 共用标注渲染路径。剪贴板与 PNG 保持 RGB32 像素，JPEG 质量为 90。
- 跨屏标注共享一个工具栏宿主；文字编辑器也只在该宿主创建。
- 导出在 GUI 线程冻结选区或标注文档快照，工作线程合成/编码；取消与原子提交共享最终同步边界，显示失效和过期请求不会提交旧结果。
- 会话析构会在工作线程结束后排空 deferred-delete，避免局部 QApplication 退出时遗留顶层 overlay。

## 自动验证

- MSVC Debug Ninja 构建成功。
- 默认自动测试 212/212 通过；真实桌面测试 4/4 通过。
- 跨屏草稿工具栏回归连续运行 5 次通过；直接/标注导出的成功、失败、取消、过期完成和析构等待均有会话测试覆盖。
- PNG 精确 round-trip 与 JPEG 解码尺寸/平均 RGB 误差阈值（不超过 12）由 SnapshotImage 测试覆盖。
- 1024 × 1024 马赛克导出并触发显示失效的隔离测试耗时约 0.30 秒（含测试进程启动），用于回归约束而非产品性能基准。
- 整分支独立审查结果：Critical 0、Important 0、Minor 0。

## 人工反馈修复（2026-09-12）

- 标注阶段继续显示锁定选区外的暗色遮罩和选区边框，不再因为进入标注模式而失去截图范围提示。
- “选择”改为“编辑”，说明其用途是选择、移动或调整已有标注；所有工具显示当前选中状态和悬停说明。
- 原来的 `3.00` 显示为“线宽 3.0 px”，只在矩形、椭圆、箭头和画笔下出现；原来的 `12` 显示为“块大小 12 px”，只在马赛克下出现并位于操作按钮之前。
- 复制、保存或切换到其他工具时自动提交正在编辑的非空文字；空白文字关闭但不创建对象，用户无需先按 `Ctrl+Enter`。
- 修复后全目标构建通过；218 个非桌面测试通过，真实桌面测试 4/4 通过。

## 当前硬件覆盖与后续兼容性验证

当前桌面测试检测到 1 台 1920 × 1080、96 × 96 DPI、SDR 显示器；捕获一帧约 109 毫秒。离屏测试覆盖合成跨屏坐标，但不替代真实多显示器、负坐标、混合 DPI 或 HDR 硬件。这些环境留到产品兼容性阶段补充，不改变本次单屏人工功能验收结论。

2026-09-14：用户确认人工功能测试通过，并在易用性反馈修复后同意合并。里程碑 3 正式完成。

## 验证安全记录（2026-09-14）

- 合并后的 MSVC Debug 全目标构建成功。
- 合并后的默认 CTest 执行到 215/218 后，Windows 发生
  `VIDEO_MEMORY_MANAGEMENT_INTERNAL (0x0000010E)` 异常重启；本次运行没有形成完整通过结论。
- 事件时间与测试重合，但现有证据不能把故障归因到某一条测试、驱动版本或硬件组件。
- 在完成独立的系统转储分析前，不在当前机器上运行真实桌面捕获、D3D、WGC 或包含这些目标的
  全量测试。里程碑 4 先使用纯逻辑与 `QT_QPA_PLATFORM=offscreen` 的 Qt 测试。
